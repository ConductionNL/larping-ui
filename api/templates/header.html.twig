<!-- HEADER -->
<header class="header">
    <nav class="nav-menuzord navbar-sticky">
        <div class="container clearfix">
            <div id="menuzord" class="menuzord menuzord-responsive">

                <a href="{{ path('app_default_index') }}" class="menuzord-brand">
                    <h1>{{ application.name }}</h1>
                </a>
                <div class="float-right btn-wrapper">
                    <a class="btn btn-outline-primary" href=" href="{{  path('app_user_login' ) }}">+ <span>Login or Join</span></a>
                </div>

                <ul class="menuzord-menu menuzord-right">

                    {% for item in menu.menuItems %}

                    <li class="">
                        <a href="{{  path( item.href ) }}">{{ item.name }}</a>
                    </li>
                    {% endfor %}

                </ul>
            </div>
        </div>
    </nav>
</header>
